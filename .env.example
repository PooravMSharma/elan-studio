DATABASE_URL="postgresql://poorav@localhost:5432/elan?schema=public"
TZ="Asia/Kolkata"

# Auth.js — generate with: npx auth secret
AUTH_SECRET=""

# WhatsApp delivery. "simulated" logs messages instead of sending;
# switch to "live" only once the platform URL and token below are real.
NOTIFY_MODE="simulated"
WA_PLATFORM_URL="http://localhost:3000"
WA_PLATFORM_TOKEN=""

# Guards /api/cron/sweep — any long random string
CRON_SECRET=""
